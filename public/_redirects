# Netlify _redirects
# Rule: serve prerendered static files first (Netlify does this automatically
# when the file exists). The SPA fallback below only fires when no static
# file matches — i.e. a route that was NOT prerendered.
# DO NOT add explicit per-route rules here; they override static file serving.

/*    /index.html   200
